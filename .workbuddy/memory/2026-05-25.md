# 2026-05-25 工作日志

## 14:10 - SQLite 历史数据保留期调整

### 变更
| 项目 | 旧值 | 新值 |
|------|------|------|
| DEFAULT_RETENTION_DAYS | 7 天 | **365 天** |
| 文件 | `src/storage/__init__.py` 第 27 行 | — |
| 预估年数据量 | — | ~22 GB（20 节点 × 秒级采集） |

### 影响
- Dashboard 历史查询、Excel 导出、调度器报表均可查询近一年数据
- DB 文件会持续增长，建议每季度检查磁盘空间
- 重启 OPCUABridge 后生效，存量数据不受影响

### 验证
- `connected=True, status=active, cache=20, nodes=20` ✅

## 14:21 - Dashboard/Excel FIQ 单位显示 + Git 推送

### 改动
| 文件 | 改动 |
|------|------|
| `dashboard.html` | FIQ 卡片值后显示 `kg` 单位；趋势图 tooltip FIQ 系列加 `kg` |
| `src/api/main.py` | Excel 历史导出：取离桶起始时间最近的值（而非覆盖取最新）；FIQ 列 number_format 加 `"kg"` 后缀 |
| `opcua_report.xlsx` | 调度器最新报表 |
| `scheduler.log` | 调度器日志 |

### Git
- `5f9dac6` → `origin/main` ✅
- 4 files changed, +4209 / -6

## 14:45 - 记忆遗漏补全
- 教训 #10 写入 MEMORY.md：Excel 导出取离桶起始时间最近的值
- Dashboard「Excel 导出」取值策略明确化

## 15:08 - 日志整合规则明确化
- 用户指出：日志要整合到**各自项目**的 MEMORY.md，非跨项目记忆
- `~/.workbuddy/MEMORY.md` 规则 #5 更新为精确表述
- OPC-UA-Bridge 项目级 MEMORY.md 补同步教训 #10

## 15:24 - 清理工作区重复记忆文件
- 删除 workspace 级 `.workbuddy/memory/` 下 4 个重复文件（与项目级完全相同）
- 权威记忆位置唯一化：`OPC-UA-Bridge/.workbuddy/memory/`

## 15:32 - 新增 10 个 ERR 节点订阅
- 节点：`FIT_05R301F01.ERR` ~ `FIT_05R310F01.ERR`
- 修改文件：`reporter/config.py` + `src/api/main.py` `_DEFAULT_NODE_IDS`
- API 预热成功，节点总数 20 → 30

### Git
- `96e2e33` → `origin/main` ✅
- 2 files changed (MEMORY.md + report.xlsx)

## 16:00~16:35 - Dashboard FIT 卡片 ERR 状态增强

### 需求
FIT 卡片 meta 行显示传感器健康状态：Quality=Good 且 ERR=0 → 绿色 Good，否则红色 Bad

### 改动文件
| 文件 | 改动 |
|------|------|
| `dashboard.html` | ① 新增 `ALL_NODES_ERR` 数组 + `ALL_BATCH_NODES`（30 节点批量查询）；② `renderCards()` FIT 卡片 meta 行改为 ERR 双重判断逻辑；③ 历史查询复选框追加 ERR 节点（红色 accent）；④ 删除 `.card.err .value` 红色样式 |
| `src/api/main.py` | `_DEFAULT_NODE_IDS` 新增 10 个 ERR 节点（此前 session 已改，本次确认） |
| `reporter/config.py` | `fixed_nodes` 新增 10 个 ERR 节点（此前 session 已改，本次确认） |

### 核心逻辑
```javascript
// FIT 卡片状态：数据有效 且 无报警 → Good
const isGood = (qual === 'Good' && errVal === 0);
```

| Quality | ERR | 显示 |
|---------|-----|------|
| Good | 0 | 🟢 Good |
| Good | 1 | 🔴 Bad |
| Good | null | 🔴 Bad |
| Bad/Uncertain | 任意 | 🔴 Bad |

### 测试
- URL 参数 `?test` 强制 R301 ERR=1 模拟 Bad，视觉确认生效后删除测试代码 ✅
- 正常模式（无 `?test`）R301~R310 全部绿色 Good ✅

### 关键决策
- 批量查询从 20 节点扩展到 30 节点（`ALL_BATCH_NODES`），但不渲染 ERR 独立卡片
- 历史查询复选框含 ERR 节点，用户可选中 ERR 查看趋势
- 不再依赖 CSS `.card.err .value` 全局红字，改为 JS 内联样式按条件着色

## 16:48 - Node-RED dashboard.html 同步

### 发现
Node-RED 的 `C:\Users\Administrator\.node-red\dashboard.html` 是独立副本（MD5 不同），上次同步 13:42，遗漏 ERR 状态改动。

### 操作
- 覆盖 `C:\Users\Administrator\.node-red\dashboard.html` ← Bridge 最新版
- MD5: `450c18397eccf05d0e72b40b01958647` 两边一致 ✅
- Node-RED Flows 无需改动（API Proxy 纯透传）

## 17:47 - 记忆文件位置整理

### 问题
发现 workspace 根目录 `C:\Users\Administrator\WorkBuddy\2026-05-21-10-09-10\.workbuddy\memory\` 下有重复的 `2026-05-25.md`（系统自动生成），与项目级 `OPC-UA-Bridge/.workbuddy/memory/` 并存。

### 操作
- 删除 workspace 根 `.workbuddy/memory/2026-05-25.md`
- 跨项目 MEMORY.md 新增规则 6：**禁止写入 workspace 根 `.workbuddy/memory/`，记忆一律写项目内**
- 未来只维护 `OPC-UA-Bridge/.workbuddy/memory/` 一处
