# 2026-04-24 工作记录

## 新增 20 个订阅节点

- 通过 batch-read API 动态订阅（无需重启桥接器）：
  - FIQ_05R301~310F01.OUT（10 个累积流量）
  - FIT_05R301~310F01.ERR（10 个错误标志）
- 20/20 全部采集成功
- ERR 节点仅订阅，不入报表

## 修复报表 FIQ 列头缺失问题

**问题**：FIQ_05R30x.OUT 已在 config.py 中（20 个节点），但 Excel 报表只显示 10 个 FIT PV 列头，FIQ 数据写到了 L~U 列但**没有列头**。

**根因**：报表文件是在只有 10 个 PV 节点时创建的，后来 config 加了 10 个 FIQ 节点，但 excel_report.py 追加逻辑不会更新列头——只追加行、不管列头是否匹配。

**修复**：
1. **excel_report.py**：追加模式新增列头匹配检测，不匹配时自动补列头
2. **当前报表文件**：手动补上 FIQ_05R301~310F01.OUT 列头（Col 12-21）
3. 重启调度器后验证：21 列数据完整，列头与值一一对应

## 当前订阅节点总览（30 个）

| 分类 | 节点 | 数量 | 报表 |
|------|------|:---:|:---:|
| 瞬时流量 PV | FIT_05R301~310F01.PV | 10 | ✅ |
| 累积流量 OUT | FIQ_05R301~310F01.OUT | 10 | ✅ |
| 错误标志 ERR | FIT_05R301~310F01.ERR | 10 | ❌ 仅订阅 |

## 清理报表残留 ERR 列

**问题**：报表 V~AE 列（第22~31列）有数据（0.0）但无列头。

**根因**：16:35 config 曾临时加入 10 个 ERR 节点，调度器跑了约 2 分钟写入 ERR 数据到 V~AE 列；16:37 ERR 从 config 删除后，这些列残留在旧报表中（openpyxl max_column 不会自动收缩），导致每次追加都触发"列头已更新: 31→21"日志。

**处理**：
1. 停 OPCUAScheduler
2. 用 openpyxl `delete_cols` 从右往左删第22~31列
3. 结果：31 列→21 列，列头完整（时间+10 PV+10 OUT）
4. 重启调度器，验证追加成功且无"列头已更新"多余日志

**ERR 订阅状态确认**：
- SQLite 有 10 个 ERR 表（h_ns_eq_1_sc_s_eq_FIT_05R30x_dot_ERR）
- 最新数据时间 2026-04-24T16:34:04，source=push
- **结论：ERR 节点已订阅，有数据推送，只是不入报表**
