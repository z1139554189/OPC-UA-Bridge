# 2026-06-11 工作日志

## 00:17 — DI 评分修复：只计坏方向

**问题**：DI 融合时未使用 `is_bad_direction`，好方向趋势（电流↓、波动↓）也被计分。
05A108 DI=23.8 但所有显著趋势全为"好方向"。
**修复**：`if w > 0:` → `if w > 0 and ft.get("is_bad_direction", True):`
**影响文件**：`scripts/motor_predictive_maintenance_report_v2.py`

---

## 00:28 — EWMA 评分修复：改用分析期中位数

**问题**：`ewma_end = ewma_values[-1]`，λ=0.15 只记得~20点，尾部波动拉偏评分。
05A108 分析期均值 12.73A，但 `ewma_end`=13.43A，EWMA 评分 14.8，健康度误报 60%。
**修复**：`ewma_end = sorted(analysis_values)[len(analysis_values) // 2]`（取分析期中位数）
**效果**：05A108 健康度从 60% 恢复到 84.9%，所有电机回到 80-85% 合理区间。

---

## 00:30 — git 推送

commit `63e73a8` 推送到 origin/main。
包含：MK稳态检测 + DI方向修复 + EWMA中位数修复 + 项目记忆文件。

---

## 00:41 — 用户确认

用户确认观察一段时间再评估，报告可信度已改善。

---

## 17:12 — 修正记忆文件路径问题

**问题**：`2026-06-11.md` 和部分 `2026-06-10.md` 内容错误写入了 `opcua_api_bridge/.workbuddy/memory/`（子目录），
而正确路径应为 `OPC-UA-Bridge/.workbuddy/memory/`。

**修正**：将缺失内容补写到正确路径，并在 MEMORY.md 中追加完整记录。
