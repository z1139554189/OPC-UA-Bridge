# 2026-06-15 工作日志

## Excel 周报边界修复

### 问题
`scripts/motor_current_excel_report.py` 第 589 行：
```python
end_time_excl = (end_dt + timedelta(seconds=1)).strftime(...)
```
`+1秒` 导致独占上限后延，6月15日 00:00:00 的数据泄漏到上周报表。自定义 `--start --end` 模式下还叠加了 `+days=1`（第565行），双重溢出更严重。

### 修复
删除 `+ timedelta(seconds=1)`，直接用 `end_dt` 作为独占上限。
修复后验证：分析周期 `2026-06-08 ~ 2026-06-14（上周）`，无 6/15 数据泄漏。

## 自动化更新 + 报告不推送规则

### 自动化更新
- 预测性维护周报自动化：脚本从 `motor_predictive_maintenance_report.py`（旧版V2）切换为 `motor_predictive_maintenance_report_v2.py`（V4.7）
- Python 路径改为 `D:/opcua_venv/Scripts/python.exe`

### Git 报告不推送规则
- 用户要求：预测维护报告 HTML 和 Excel 周报不推送 git
- `.gitignore` 新增 `opcua_api_bridge/reports/` 模式
- 已提交的旧报告文件通过 `git rm --cached` 从跟踪中移除
- 跨项目记忆 `~/.workbuddy/MEMORY.md` 新增规则 #9

---
