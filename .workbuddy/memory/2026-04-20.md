# 2026-04-20 工作记录

## 生成项目技术文档

- 将项目技术要点、踩坑实录、架构方案整理成完整 HTML 文档
- 文件路径：`C:\Users\Administrator\Desktop\OPC-UA-Bridge-技术文档.html`
- 涵盖内容：
  - 项目概览（技术栈、目录、GitHub）
  - 系统架构图（ASCII 风格，含4个Task协同关系）
  - 模块清单（main.py v3 / client.py v7 完整说明）
  - OPC UA 服务器特性与三大硬性限制
  - 采集控制逻辑（状态机伪代码 + 超时参数表）
  - 数据流向（push/heartbeat/cloud_sync 三条路径）
  - 云端部署（TDSQL-C / SCF / 看板）
  - Windows 服务（NSSM 配置与管理命令）
  - 踩坑实录（9 个典型坑，含根因+解法）
  - 经验教训 & 最佳实践
  - 待处理事项

## 修正技术文档 — 采集状态机与实际代码不符

用户指出桌面 HTML 文档中的**采集状态机表和伪代码仍是旧版逻辑**，与 client.py v7.0.0 实际代码不一致。已修正以下内容：

### 旧版 vs v7.0.0 关键差异

| 项目 | 旧版（已废弃） | v7.0.0（当前代码） |
|------|:---:|:---:|
| `yielded` 状态 | ✅ 有，Session 满自动让出 | ❌ 移除 |
| `_yielded` 属性 | ✅ 控制让出/恢复 | ❌ 不存在 |
| Session 满处理 | 让出 + 每30秒重试 | `os._exit(2)` 终止进程，禁止自动重启 |
| 退避控制 | 多个变量（session_full_retry_at, yielded 等） | 统一用 `_retry_after` 时间戳 |
| YIELD_RECHECK_INTERVAL | 30秒 | 不再需要 |
| Session 满退避 | 300秒 | 直接退出进程 |
| PUSH_FAIL_BACKOFF | 无（用60s） | **500s**（给 OPC 服务器足够时间释放旧 Session） |
| CONNECT_FAIL_BACKOFF | 无 | **1800s**（30分钟） |
| 新增 backoff 状态 | ❌ | ✅ `_client is None + _retry_after > 0` |

### 修改的文档位置（共 6 处）

1. **状态机表格**：移除 `yielded` 行，新增 `backoff(Ns)` 和 `reconnecting`，增加 v7 变化说明框
2. **Task 表格描述**："Session 让出与恢复" → "Session 满终止进程"
3. **_collect_loop 伪代码**：完全重写为 v7 极简退避版真实逻辑
4. **超时参数表**：移除 `YIELD_RECHECK_INTERVAL`/`Session满退避(300s)`，新增 `PUSH_FAIL_BACKOFF`(500s) / `CONNECT_FAIL_BACKOFF`(1800s)
5. **坑 2 解法**：自适应让出 → os._exit(2) 终止策略，更新代码示例
6. **坑 4 解法**：补充极简退避模型说明
7. **Session 上限描述**：自适应让出 → 终止策略
8. **经验教训表**：Session 设计原则更新
