# 2026-06-05 工作日志

## 13:24 - 新增 IIAS 搅拌电机电流节点 + 测试版4

### 背景
用户要求新增磺化釜搅拌电机（05A102~05A111）的电流信号及故障信号共20个节点，并创建测试版4 dashboard 展示。

### 新增节点（共 20 个）

| 类型 | 节点 | 说明 |
|------|------|------|
| IIAS.PV | IIAS_05A102.PV ~ IIAS_05A111.PV（10个） | 搅拌电机电流，量程 0-50A |
| IIAS.ERR | IIAS_05A102.ERR ~ IIAS_05A111.ERR（10个） | 电流故障信号（0=正常，1=故障） |

### 改动文件

| 文件 | 改动 |
|------|------|
| `reporter/config.py` | `fixed_nodes` 末尾追加 20 个 IIAS 节点（含注释） |
| `src/api/main.py` | `_DEFAULT_NODE_IDS` 追加 20 个 IIAS 节点；新增 `/dashboard_test4` 路由 |
| `dashboard_test4.html` | 复制自 dashboard.html V1.1，新增 IIAS 卡片显示（蓝色，单位 A）；CARD_GROUPS/NODE_GROUPS 新增 IIAS 分组；ERR 判断支持三类型（FIT/FIQ/IIAS）；弹窗单位扩展 A |

### 验证结果
- Bridge 重启后 `cache_size: 50`（30 旧节点 + 20 IIAS），说明节点订阅成功
- OPC UA 连接正常（opcua_connected: true）

### 后续说明
- 10 个 IIAS 电流信号用于后续电机健康度模块（预测性维护）
- test4 路由：`http://localhost:8000/dashboard_test4`

## 13:36 - 修复历史数据导出 IIAS 单位缺失

### 问题
`batch_history_export` 表头单位判断只覆盖 FIT/FIQ/ERR 三种类型，IIAS 节点导出时列头只显示裸节点名（如 `IIAS_05A102.PV`），缺少 `(A)` 单位。

### 修复
`src/api/main.py` 第 480 行后追加：
```python
elif "IIAS" in display.upper() and "ERR" not in display.upper():
    display = f"{display} (A)"
```

| 类型 | 单位 |
|------|------|
| FIQ | (kg) |
| FIT | (kg/h) |
| IIAS | (A) ← 新增 |
| ERR | (0/1) |

### 服务重启
- kill 旧 uvicorn 进程，等 30s 释放 OPC UA session，重启成功
- 50 个节点全部订阅正常

## 13:53 - 修复 cardSelectAll 漏掉 IIAS + 新增登录鉴权

### cardSelectAll 修复
`cardSelectAll(state)` 只遍历 `[...ALL_NODES_FIT, ...ALL_NODES_FIQ]`，遗漏 IIAS 节点，导致全选/清空对 IIAS 卡片无效。已追加 `...ALL_NODES_IIAS`。

### 登录鉴权（dashboard_test4.html）
- 新增登录遮罩层，需输入账号密码后才显示主界面
- 账号：`admin`，密码：`opcua2024`
- 支持回车快捷登录，错误提示
- 纯前端验证，测试版专用，不涉及后端/Node-RED

## 14:11 - 发布正式版 V1.2

### 合并内容
将 test4 的 IIAS 支持合并到正式版 `dashboard.html`，涉及 12 处代码变更：
- `ALL_NODES_IIAS` / `ALL_NODES_IIAS_ERR` 数组定义
- `ALL_NODES` 和 `ALL_BATCH_NODES` 展开追加 IIAS
- `NODE_GROUPS` 新增"IIAS 搅拌电机电流"+"IIAS ERR 电流故障"
- `CARD_GROUPS` 新增"IIAS 搅拌电机电流"（蓝色 `#58a6ff`）
- `selectAll` / `cardSelectAll` / `cardUpdateLabel` 补上 IIAS 节点
- `renderCards` 新增 `iiasErrVals` 映射 + `getErrForIias` 函数，cat/unit 改为三元分支
- `openPopup` / `exportPopupExcel` / `updatePopupRtChart` / `queryPopupHistory` 四个函数 cat/unit 改为三元分支

### 排除项
- 登录鉴权（测试版专用，不合并）

### 同步
- 双端同步：OPC-UA-Bridge 仓库 + Node-RED (~/.node-red/) 均已提交

## 14:21 - 正式版补充登录鉴权，密码改为 Admin_00

- 将登录鉴权从 test4 合并到正式版 `dashboard.html`（CSS + HTML overlay + JS）
- 密码：`Admin_00`（区分大小写），账号不变：`admin`
- 副标题改为"V1.2 — 请输入授权账号登录"
- 双端同步：OPC-UA-Bridge + Node-RED 均已提交
