# 2026-04-02 操作日志

## 桥接器 & 调度器状态检查（12:39-12:50）

### 检查结果
- **桥接器** (PID 11068)：✅ 正常，8000 端口，OPC UA 连接正常，内存 88.8MB，已运行约 19h
- **调度器**：发现双实例（PID 12640 + 14032），已 kill 12640，保留 14032
- 当前调度器参数：`--interval 5`（每 5 分钟采集一次）

### 旧 Bug 结论
- `scheduler.log` 里记录的 `'str' object has no attribute 'get'` 错误是旧日志（连的是 8001 端口），当前版本无此问题

### 手动测试
- 执行 `reporter/run_report.py` 成功，6 个 HTTP 请求全部 200 OK
- 报表生成至 `reporter/output/opcua_report_20260402_124623.xlsx`
- reporter/output/ 现有 3 个报表文件

## 调度器日志分析与 Bug 修复（13:05-13:15）

### 日志分析结果（594 次采集）
- 总计 594 次采集：成功 284，失败 310，**成功率仅 48%**
- 4 种错误类型：
  - **MergedCell (262x)**：append 模式清空合并单元格报错，占 84%
  - **str.get (41x)**：早期 Bug，已修复
  - **Permission denied (17x)**：Excel 文件被占用
  - **gbk codec (1x)**：日志 emoji 编码问题

### 修复内容
1. **`excel_report.py`**：重写 append 模式逻辑
   - 旧逻辑：每次追加都清空并重建所有 4 个 Sheet（摘要/实时值/历史趋势/历史原始）→ 碰到合并单元格就崩
   - 新逻辑：首次新建全部 4 个 Sheet，后续追加只写"历史原始" Sheet 的新行，其他 Sheet 保持快照不变
2. **`scheduler.py`**：PermissionError 单独捕获为 warning，不再 crash
3. 验证：新建 + 2 次追加测试全部通过，无报错

### 调度器状态
- 旧调度器 (PID 14032) 已终止，新调度器已启动（hidden 窗口，`--interval 5`）
- scheduler.log 已清理，新日志从 13:12 开始

## 报表重新设计（13:24-13:30）

### 用户需求
1. 每分钟记录一次数据，不管值是否变化
2. 数据横向（节点列），时间纵向（行向下增长）
3. 5 个节点：FIT_05R301~305F01.PV

### 重写内容
- **config.py**：节点改为 5 个 PV，间隔 1 分钟，固定文件名 `opcua_report.xlsx`
- **excel_report.py**：完全重写，只做 1 个 Sheet（实时数据），每行 = 时间 + 5 个值
- **scheduler.py**：简化，去掉历史查询，只采实时值+追加，加成功/失败计数
- 修复笔误：`__handle_signal` → `_handle_signal`
- 修复启动方式：`Start-Process -WorkingDirectory` 不生效，改用 `cmd /c start /B /D`

### 验证
- 新建 + 2 次追加测试通过
- 调度器运行中，首次 13:27:23，追加 13:28:27（间隔约 1 分钟）

## 调度器重构：HTTP API → 直读 SQLite（15:09）

### 背景
- 架构评审（FastAPI 专家确认）：调度器通过 HTTP API 拿数据是多余的，SQLite 里已有全部数据
- 改为调度器直接读 SQLite，去掉 httpx 依赖，减少故障点

### 改动
- **scheduler.py** 完全重写：
  - 去掉 `OpcUaCollector` / `httpx` 调用
  - 新增 `read_latest_values()`：用 `sqlite3` 直读每个节点的最新一条记录
  - 数据格式保持与原 batch_read API 一致，`generate_excel()` 无需修改
  - 新增 `--db` 参数指定 SQLite 路径

### 验证
- `--once` 测试：新建 + 追加均成功，数据正确（398.65, -0.35, -0.35, -1.05, -9.8）
- 后台调度器已启动，每分钟自动追加
- 旧调度器（PID 15880）已终止，旧报表已删除重新开始

## 历史数据报表脚本（13:36）

### 新增文件
- **`reporter/history_report.py`**：一次性历史报表生成器
  - 从 SQLite 历史库读取过去 1 小时数据（可配置 `HOURS_BACK`）
  - 5 个 PV 节点，按分钟采样（每分钟取最后一个值）
  - 布局：横向节点列，纵向时间行，含折线图
  - 输出：`opcua_history_YYYYMMDD_HHmmss.xlsx`

### 注意事项
- SQLite 表名前缀实际为 `h_ns_eq_1_sc_s_eq_`（比 `_safe_table_name` 生成的多一个 `s_eq_`）
- 5 个 PV 节点从 13:24 开始订阅，目前只有约 12 分钟历史数据
- 脚本独立运行，不影响定时调度器

## 容错加固（15:25）

### scheduler.py
- 新增 `run_once_with_retry()`：单次采集失败自动重试最多 3 次，间隔 5 秒
- 新增 `alert.log`：独立告警日志文件（WARNING 级别及以上）
- 连续失败 3 次触发告警，每 10 次持续告警一次（避免刷屏）
- SQLite 连接加 `timeout=10`，连接失败抛明确错误信息
- 恢复时自动打印日志

### main.py（桥接器）
- 新增 `_reconnect_monitor()`：后台 asyncio 任务，每 30 秒检查 OPC UA 连接
- 断连自动重建客户端并重连，无限重试（`RECONNECT_MAX_RETRIES=0`）
- 全局异常处理器加 `exc_info=True`，方便排查
- 关闭时正确取消后台任务

### 测试
- 旧调度器（PID 14012）已终止
- 新调度器 `--once` 测试通过（第 21 行追加成功）
- 后台调度器已启动

## NSSM 注册 Windows 服务（15:35-15:57）

### 安装
- NSSM 下载到 `D:\Tools\nssm.exe`（nssm 2.24-101-g897c7ad）

### 注册的服务
- **OPCUABridge**（OPC UA REST API Bridge）
  - 命令：`C:\Python314\python.exe -m uvicorn src.api.main:app --host 0.0.0.0 --port 8001`
  - 工作目录：`opcua_api_bridge/`
  - 日志：`logs/bridge_stdout.log` + `logs/bridge_stderr.log`
  - 端口改为 **8001**（旧的 8000 被僵尸进程 PID 11068 占用，无法 kill，需重启电脑才能释放）
  - 崩溃重启延迟：60 秒，自动启动：是
- **OPCUAScheduler**（OPC UA Report Scheduler）
  - 命令：`C:\Python314\python.exe reporter/scheduler.py`
  - 日志：`logs/scheduler_stdout.log` + `logs/scheduler_stderr.log`
  - 崩溃重启延迟：5 秒，自动启动：是

### 管理命令
- `net stop OPCUABridge` / `net start OPCUABridge`
- `net stop OPCUAScheduler` / `net start OPCUAScheduler`
- `services.msc` 图形界面管理

### 注意事项
- PID 11068 的僵尸进程占着 8000 端口，手动 kill/PowerShell/ctypes 全部失败，需重启电脑释放
- 桥接器端口已改为 8001，collector.py 等引用 8000 的地方需同步更新

## 重启后恢复 + Python 版本降级（16:20-16:45）

### 重启后状态恢复
- 两个 NSSM 服务自动启动正常（SERVICE_AUTO_START）
- 端口 8000 释放，桥接器改回 8000
- **NSSM AppParameters 引号问题**：`nssm set` 命令引号转义导致参数多了 `\`，改用 Python `winreg` 直接写注册表修正

### 路由丢失
- 重启后手动加的路由 `172.30.0.0/24 → 192.168.10.1` 丢了
- 已改为永久路由 `route -p add`

### Python 3.14 → 3.13 降级
- **原因**：asyncua 1.1.8 在 Python 3.14 上报 `issubclass() arg 1 must be a class`（GitHub issue #1880，open 状态）
- **操作**：
  1. 卸载 Python 3.14.3（MSI 组件逐个 msiexec /x，残留目录手动 rmdir）
  2. 安装 Python 3.13.3（`C:\Program Files\Python313\python.exe`，静默安装 + Add PATH + pip）
  3. pip install 所有项目依赖（asyncua, fastapi, uvicorn, openpyxl, playwright 等 30+ 包）
  4. `playwright install chromium`
  5. 修改 NSSM 注册表 Application → `C:\Program Files\Python313\python.exe`（Python winreg）
  6. 回退 `client.py` 的 Python 3.14 monkey patch 代码
- **验证**：
  - `/health` → `opcua_connected: true` ✅
  - `/api/v1/nodes/batch-read` → 真实值（PV=0.4, FLAG=128, AOF=False, quality=Good）✅

### 清理
- 删除所有 `_debug_*.py` 调试脚本
- 删除 `collector.py`、`run_report.py`、`run_realtime_report.py`（旧 HTTP 采集方案，已被直读 SQLite 替代）
- 更新 `config.py`（去掉 `api_url`）、`README.md`（适配当前架构）

### 教训记录
- **遇错先查**：遇到库兼容性报错，第一时间查 GitHub Issues，不要先写 monkey patch
- **先想后做**：执行影响系统环境的大操作前，先评估影响范围

## 报表数据不变问题修复（16:49-16:56）

### 根因
- 调度器直读 SQLite，但 SQLite 里的 R301-R305 节点数据停在 15:08
- 桥接器用**按需订阅**：只有 API 被请求过的节点才会被订阅并写入 SQLite
- R301-R305 只在调度器 SQLite 直读路径里出现，从未被 API 请求过，所以桥接器从未订阅
- 桥接器重启后订阅全部清空，只有之前通过 API 请求过的 R201 等节点被重新订阅

### 修复
- `scheduler.py` 新增 `warmup_subscriptions()` 函数：启动时先调桥接器 API `/api/v1/nodes/batch-read` 触发订阅
- 预热失败不影响调度器运行（fallback 到纯 SQLite 直读）
- 重启调度器后验证：5 个 R3xx 节点全部有持续新数据写入 SQLite ✅
